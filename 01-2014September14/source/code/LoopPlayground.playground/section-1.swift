// Playground - noun: a place where people can play

import UIKit

for brickIndex in 1...3 {
    200 * brickIndex + 1
    let xPos = 160/3 * CGFloat(brickIndex + 1.0)
    xPos
    
    
    let screenWidth = 320
    let brickWidth = 90
    let brickQty = 3
    let brickMargin = 0
    
    
    brickQty * (brickWidth + brickMargin)
    
    screenWidth / 2)
    
    ((screenWidth / brickQty) + brickWidth/2) * brickIndex

    (screenWidth / brickQty + brickWidth/2) * brickIndex
}



